import { Request, Response } from 'express';

export const getAttendance = async (req: Request, res: Response) => res.json([]);
export const markAttendance = async (req: Request, res: Response) => res.json({});

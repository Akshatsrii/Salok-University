import { Request, Response } from 'express';

export const getTimetable = async (req: Request, res: Response) => res.json([]);
export const generateTimetable = async (req: Request, res: Response) => res.json({});
